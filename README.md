# QADataPlanning22.7.26

Summary:
1. Set Up SSH Key on our local machine
2. On Git Hub we then added the .pub SSH key
3. We created a new Repo on Github
4. We cloned the Repo via the SSH url (not https). This is because SSH is more secure

Commands to learn:

- CLI:
cd - change directory
ls - show files
mkdir - make directory
pwd - print working directory
cat - copy


- Git:
git clone <url>

git add <files>
git commit -m "your message here" - you need a message with speech marks
git push 



Just adding some new text as required


